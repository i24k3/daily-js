



let students = ["izake",
                "sukuna",
                "ladoo",
                "lawrance",
                "osama",
                "candice",
                "qasim"
              ];


//for (let index = 0; index< students.length; index++{
  //const element = students[index];
  //console.log(element);
  //}

// the of gives the element while in gives the index

for (student in students)
{
  const stud = students[student];
  console.log(stud);
}
