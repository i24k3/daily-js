
function milesToKms(miles) {
  // 1 mile = 1.60934 km
  const kms = miles / 1.60934;
  return kms;
}

function kmsToMiles(km_s) {
  // 1 mile = 1.60934 km
  const miles = kms * 1.60934;
  return miles;
}

function inchesToCms(inches) {
  const cms = inches * 2.54;
  return cms;
}
function cmToMeters(cm) {
  return cm / 100;
}

function poundToKilo(pounds) {
  const kilos = pounds / 2.2046;
  return kilos;
}

function bmi() {
  cosnt bmi = kgWeight / (meterHeight ** 2)
}

